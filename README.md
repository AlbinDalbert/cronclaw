file structure example:

.cronclaw/
  config.yaml
  pipelines/
    morning-news/
      pipeline.yaml
      state.json
      workspace/
        summary.md (example file)
        audio.wav (example file)
    weekly-report/
      pipeline.yaml
      state.json
      workspace/
